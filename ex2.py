print(
    '''                                    Exercise \#2
    Comment that line, which contains pygame.draw.rect(...., (0,0,0), .... ) for remove black square
    Comment that line, which contains pygame.draw.circle(...., (0,0,255), .... ) for remove blue ball''')